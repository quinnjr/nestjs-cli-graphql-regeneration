{
  "$schema": "http://json-schema.org/schema",
  "$id": "ClosedSchemaFixture",
  "title": "Closed schema fixture (negative control)",
  "type": "object",
  "properties": {
    "name": { "type": "string" }
  },
  "additionalProperties": false
}
